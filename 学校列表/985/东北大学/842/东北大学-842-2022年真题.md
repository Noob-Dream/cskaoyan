# 东北大学2022年842真题

```
计算机842数据结构算法题1
把单链表中值最大的节点移到链尾
题2
找到第k层中度为1的节点个数
题3
找出图中的最长路径长度,并输出最长路径
简答题1
给出堆排序的初始堆(小顶堆)
写出堆排序前两躺结果。

操作系统
给了两个位示图,找出分配了哪十块空闲块
块大小256字节,块号两字节。文件物理结构为混合式索引,O到2为直接索引，3为间接索引。
请给出盘块结构和文件结构
C语言算法题
(1)编写void stuff(char*s1,int left,intright,char *s2)将s1中left到right(含)的字符替换为s2,如

(2)编写主函数,读取两个字符串和两个整数,输出结果

```
